feedparser is a python module for downloading and parsing syndicated feeds. The team will use the module for handling RSS feeds. 

Requirements:
- Python 2.4+ (let's not do Python 3)

Notes:
- contained in feedparser.py
- one primary public function, parse (takes # of args, but only 1 is required, and it can be a URL, a local filename, or a raw string containing feed data in any format)
